---
ANDROID_DEVICE_IMAGE: zebrunner/mcloud-device
ANDROID_DEVICE_VERSION: 2.0-beta52

APPIUM_IMAGE: public.ecr.aws/zebrunner/appium
APPIUM_VERSION: 1.0-beta1

#TODO: test if we can remove it as video recording moved outside the stf device container
# to fix "FATAL ERROR: CALL_AND_RETRY_LAST Allocation failed - JavaScript heap out of memory" during video recording
MAX_OLD_SPACE_SIZE_VALUE: 8172

DEVELOPER_IMAGES_VERSION: latest
