{% set added_ids = [] %}
{% for device in devices %}
{%   if device.id not in added_ids: %}
{%     set added_ids = added_ids.append(device.id) %}
{{     device.name }}|{{ device.os }}|{{ device.id }}|{{ device.adb_port }}|{{ device.min_port }}|{{ device.max_port }}|{{ device.proxy_port | default('0') }}|{{ device.appium_port }>
{%   endif %}
{% endfor %}
